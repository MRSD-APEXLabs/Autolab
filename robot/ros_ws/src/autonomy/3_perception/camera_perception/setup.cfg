[develop]
script_dir=$base/lib/camera_perception
[install]
install_scripts=$base/lib/camera_perception
[build_scripts]
# run with the python3 first on PATH, so an activated virtualenv is used
executable=/usr/bin/env python3
[tool:pytest]
# colcon test runs pytest in the package folder; only collect test/
testpaths = test
